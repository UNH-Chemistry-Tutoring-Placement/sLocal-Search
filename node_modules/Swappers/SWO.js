module.exports = SWO;

var swap = require('Swappers'),
    data = require('FileData').get(),
    grader = require('Grade');

function SWO(){
    var students = data.student.students
    var actualPossibleTimes = []
    var times = data.class.groups
    for(var i in times) console.log("time: " + times[i])
}