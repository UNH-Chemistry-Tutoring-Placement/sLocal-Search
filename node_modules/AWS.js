module.exports = AWS;

var swap = require('Swap');

function AWS(){
    var results = []
//-----------------------Want to do this in a for loop but having issues-----------------------------------------------
    var test1 = swap()
    var test2 = swap()
    var test3 = swap()
    var test4 = swap()
    var test5 = swap()

    results.push(test1)
    results.push(test2)
    results.push(test3)
    results.push(test4)
    results.push(test5)
//---------------------------------------------------------------------------------------------------------------------

    results.sort(function(a,b){return a.grade- b.grade});
    var x;
    for(x in results){
        console.log("Grade: " + results[x].grade)
    }

}

