module.exports = ClassParser;

var parser = require('Parser');

function ClassParser(input){
	var p = new parser(input);
	
	var data = {};
	
	data.format = Number(p.getSecondPart());
	data.description = p.getSecondPart();
	data.numberOfLectures = p.getSecondPartAsNumber();
	data.lectures = [];
	for (var i = 0; i < data.numberOfLectures; i++){
		data.lectures.push(p.getSecondPart());
	}
	data.numberOfGroups = p.getSecondPartAsNumber();
	data.groups = {};
    
	for (var i = 0; i < data.numberOfGroups; i++){
		var group = {
			name: p.getSecondPart(),
			email: p.getSecondPart(),
			time: p.getSecondPart()
		};
        
        data.groups[group.time] = group;
	}
	
	return data;
}
